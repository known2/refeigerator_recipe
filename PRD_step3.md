# PRD - Step 3: 사용자 프로필 및 레시피 저장

## 1. 개요
사용자가 자신의 계정(프로필)을 만들고, 2단계에서 생성된 레시피를 저장/관리할 수 있게 하는 기능. 1, 2단계가 단발성 인식/생성 흐름이었다면, 3단계는 재방문 사용자를 위한 영속성(persistence)을 더한다.

## 2. 목표
- 사용자가 마음에 드는 레시피를 계정에 저장하고 나중에 다시 볼 수 있게 한다.
- 사용자별 선호도(알레르기, 선호 요리 종류 등)를 프로필에 저장해 2단계 추천 품질을 개선할 수 있는 기반을 마련한다.

## 3. 사용자 스토리
- 사용자로서, 회원가입/로그인 후 내가 만들었던 레시피 추천 기록을 다시 보고 싶다.
- 사용자로서, 마음에 드는 레시피를 "저장"해서 나만의 레시피북을 만들고 싶다.
- 사용자로서, 저장한 레시피를 삭제하거나 메모를 남기고 싶다.
- 사용자로서, 내 알레르기/비선호 재료를 프로필에 등록해두면 매번 입력하지 않아도 됐으면 좋겠다.

## 4. 범위 (In Scope)
- 회원가입/로그인/로그아웃 (이메일+비밀번호 기본, 필요 시 소셜 로그인은 추후 검토)
- 사용자 프로필: 닉네임, 알레르기/제외 재료, 선호 요리 종류(선택 입력)
- 레시피 저장/삭제 (2단계에서 생성된 레시피를 "찜"하듯 저장)
- 저장된 레시피 목록(마이페이지) 조회
- 저장된 레시피에 개인 메모 추가 (선택)
- 프로필의 알레르기/선호도를 2단계 레시피 생성 프롬프트에 자동 반영 (로그인 상태일 때)

## 5. 범위 제외 (Out of Scope)
- 소셜 기능(레시피 공유, 팔로우, 댓글 등)
- 결제/구독 기능
- 소셜 로그인(OAuth) — 1차 버전은 이메일 기반 인증만 지원, 추후 별도 검토

## 6. 기능 요구사항

### 6.1 인증
- 이메일/비밀번호 기반 회원가입, 로그인, 로그아웃
- 비밀번호는 해시(bcrypt 등)로 저장, 평문 저장 금지
- 세션/토큰 기반 인증(JWT 또는 세션 쿠키) 적용
- 비로그인 사용자도 1, 2단계(인식/추천)는 계속 이용 가능하되, "저장" 기능만 로그인 필요

### 6.2 사용자 프로필
- 프로필 항목: 닉네임, 이메일, 알레르기/제외 재료 목록, 선호 요리 종류(한식/양식/중식/일식 등 다중 선택), 선호 난이도
- 프로필 수정 화면 제공
- 프로필의 알레르기/제외 재료는 2단계 레시피 생성 요청 시 자동으로 프롬프트에 포함되어, 해당 재료가 들어간 레시피는 제외되도록 한다.

### 6.3 레시피 저장
- 2단계 레시피 상세 화면의 "저장하기" 버튼 활성화 (로그인 상태일 때)
- 저장 데이터: 레시피 제목, 재료 목록(보유/부족), 조리 순서, 생성 당시 원본 이미지(선택, 용량 고려하여 썸네일만 저장 검토), 저장 일시
- 비로그인 상태에서 "저장하기" 클릭 시 로그인 유도 모달 표시

### 6.4 마이페이지 (저장된 레시피 관리)
- 저장된 레시피 목록을 카드 형태로 표시 (최신순 기본 정렬)
- 레시피 클릭 시 상세보기 (2단계 상세 화면과 동일한 레이아웃 재사용)
- 레시피 삭제 기능
- 레시피별 개인 메모 추가/수정 (선택 기능)
- 저장 레시피 검색/필터(제목, 요리 종류) — 1차 버전에서는 간단한 텍스트 검색만 지원

### 6.5 에러 처리
- 중복 이메일 가입 시도 시 안내 메시지
- 로그인 실패(잘못된 비밀번호 등) 시 안내 메시지
- 세션 만료 시 재로그인 유도

## 7. 데이터 모델 (초안)

```
User
- id
- email (unique)
- passwordHash
- nickname
- allergies: string[]
- preferredCuisines: string[]
- preferredDifficulty: string
- createdAt

SavedRecipe
- id
- userId (FK -> User.id)
- title
- usedIngredients: string[]
- missingIngredients: string[]
- steps: string[]
- difficulty
- cookTimeMinutes
- memo
- sourceImageThumbnail (optional)
- savedAt
```

## 8. 비기능 요구사항
- 비밀번호는 반드시 해시 저장, 평문 로그 금지
- 사용자 데이터 접근 시 인증/인가 검증 (본인 데이터만 조회/수정/삭제 가능)
- 세션/토큰 만료 정책 수립 (예: 7일)
- DB는 초기 단계에서 가벼운 선택지(SQLite/PostgreSQL 등) 사용 가능, 추후 확장 고려

## 9. 화면 구성 (초안)
1. 로그인/회원가입 화면
2. 프로필 설정 화면 (알레르기, 선호 요리 종류 등)
3. 마이페이지: 저장된 레시피 목록
4. 저장된 레시피 상세 화면 (메모 편집 포함)
5. 2단계 레시피 상세 화면 내 "저장하기" 버튼 연동

## 10. 완료 기준 (Acceptance Criteria)
- [ ] 사용자가 이메일/비밀번호로 회원가입 및 로그인할 수 있다.
- [ ] 로그인한 사용자가 프로필(알레르기, 선호 요리 종류)을 설정/수정할 수 있다.
- [ ] 로그인한 사용자가 2단계에서 생성된 레시피를 저장할 수 있다.
- [ ] 비로그인 사용자가 저장 시도 시 로그인 유도가 표시된다.
- [ ] 마이페이지에서 저장된 레시피 목록을 확인하고 상세보기할 수 있다.
- [ ] 저장된 레시피를 삭제할 수 있다.
- [ ] 프로필에 등록한 알레르기 재료가 2단계 레시피 추천 시 자동으로 제외 조건에 반영된다.
- [ ] 타 사용자의 저장 레시피에 접근할 수 없다(인가 검증).

## 11. 구현 참고 사항
- 인증은 자체 구현(bcrypt+JWT/세션) 또는 Auth 라이브러리(NextAuth, Supabase Auth 등) 사용을 검토한다.
- 1~2단계는 OpenRouter API 키만 있으면 동작했지만, 3단계부터는 별도의 데이터베이스와 인증 인프라가 필요하므로 배포 환경(DB 호스팅 등)을 함께 결정해야 한다.
- 알레르기 필터를 2단계 프롬프트에 반영할 때, [[PRD_step2]]의 프롬프트 템플릿에 "제외할 재료" 파라미터를 추가하는 방식으로 연동한다.
