# PRD - Step 1: 냉장고 사진 업로드 및 재료 인식

## 1. 개요
사용자가 냉장고(또는 식재료) 사진을 업로드하면, OpenRouter API를 통해 `google/gemma-4-26b-a4b-it:free` 비전 모델이 이미지를 분석하여 사진 속 식재료 목록을 텍스트로 추출해주는 기능. 본 프로젝트의 1단계로, 2단계(레시피 생성)의 입력 데이터를 만드는 것이 핵심 목적이다.

## 2. 목표
- 사용자가 별도의 텍스트 입력 없이 사진 한 장으로 보유 식재료 목록을 얻는다.
- 인식된 재료 목록을 구조화된 형태(JSON 등)로 저장하여 2단계에서 재사용할 수 있게 한다.

## 3. 사용자 스토리
- 사용자로서, 냉장고를 열어 사진을 찍어 업로드하면 안에 뭐가 있는지 자동으로 목록화되었으면 좋겠다.
- 사용자로서, 인식이 잘못된 재료가 있으면 직접 수정하거나 삭제/추가할 수 있었으면 좋겠다.

## 4. 범위 (In Scope)
- 이미지 업로드 UI (드래그 앤 드롭 / 파일 선택 / 모바일 카메라 촬영)
- 업로드 이미지 미리보기
- OpenRouter API 호출을 통한 재료 인식 (`google/gemma-4-26b-a4b-it:free`)
- 인식 결과를 재료명 리스트(가능하면 수량/단위 포함)로 화면에 표시
- 인식 결과 수동 편집 (추가/삭제/이름 수정)
- 인식 실패/에러 상태 처리

## 5. 범위 제외 (Out of Scope)
- 레시피 생성 (2단계에서 다룸)
- 사용자 계정/로그인, 데이터 영구 저장 (3단계에서 다룸)
- 여러 장의 이미지 동시 업로드 (1차 버전에서는 1장만 지원)

## 6. 기능 요구사항

### 6.1 이미지 업로드
- 지원 포맷: JPEG, PNG, WebP
- 최대 파일 크기: 10MB (초과 시 사용자에게 안내 메시지 표시)
- 업로드 즉시 클라이언트 측에서 미리보기 표시
- 업로드 중 로딩 인디케이터 표시

### 6.2 재료 인식 (OpenRouter 연동)
- 백엔드(API Route/서버)에서 OpenRouter Chat Completions API 호출
  - 모델: `google/gemma-4-26b-a4b-it:free`
  - 이미지는 base64 data URL 또는 공개 URL 형태로 `image_url` 콘텐츠 파트에 담아 전송
  - 프롬프트 예시: "이 이미지에서 보이는 식재료를 모두 나열하고, 각 재료명과 대략적인 수량/상태(예: 신선함, 반 정도 남음)를 JSON 배열로만 응답하라."
  - 응답은 아래와 같은 JSON 스키마를 기대:
    ```json
    {
      "ingredients": [
        { "name": "계란", "quantity": "6개", "note": "신선" },
        { "name": "대파", "quantity": "1대", "note": "" }
      ]
    }
    ```
  - 모델 응답이 JSON 형식이 아니거나 파싱 실패 시, 재시도 1회 또는 사용자에게 "인식 실패, 다시 시도해주세요" 안내
- API 키(`OPENROUTER_API_KEY`)는 서버 측 환경 변수로만 사용하고 클라이언트에 노출하지 않는다.
- OpenRouter 호출 시 `HTTP-Referer`, `X-Title` 헤더 권장 설정 (OpenRouter 정책 준수)

### 6.3 결과 표시 및 편집
- 인식된 재료를 리스트/카드 형태로 표시 (이름, 수량, 상태 메모)
- 각 항목에 대해 삭제 버튼 제공
- 재료명/수량 직접 수정 가능 (인라인 편집)
- "재료 추가" 버튼으로 인식되지 않은 재료 수동 입력 가능
- 최종 확정된 재료 리스트를 다음 단계(2단계)로 넘길 수 있는 상태로 유지 (예: 세션/state 저장)

### 6.4 에러 처리
- 이미지 미업로드 상태에서 인식 요청 시 버튼 비활성화
- OpenRouter API 오류(429 rate limit, 5xx 등) 시 사용자에게 친화적 에러 메시지 표시
- 무료 모델(`:free`) 특성상 레이트 리밋 발생 가능 → 재시도 안내 및 백오프 로직 고려

## 7. 비기능 요구사항
- 이미지 업로드~인식 결과 표시까지 평균 10초 이내 목표 (무료 모델 응답 속도에 따라 변동 가능)
- API 키 등 민감 정보는 `.env`로 관리, 저장소에 커밋 금지
- 클라이언트에서 직접 OpenRouter를 호출하지 않고 반드시 백엔드를 경유 (API 키 보호)

## 8. 화면 구성 (초안)
1. 메인 화면: 이미지 업로드 영역 + "재료 인식하기" 버튼
2. 결과 화면: 업로드한 이미지 썸네일 + 인식된 재료 리스트(편집 가능) + "레시피 추천받기" 버튼(2단계 진입, 현재는 비활성 또는 placeholder)

## 9. 완료 기준 (Acceptance Criteria)
- [ ] 사용자가 이미지를 업로드하면 미리보기가 표시된다.
- [ ] "재료 인식하기" 클릭 시 OpenRouter API가 호출되고 로딩 상태가 표시된다.
- [ ] 인식 성공 시 재료명 리스트가 화면에 표시된다.
- [ ] 사용자가 재료를 추가/삭제/수정할 수 있다.
- [ ] API 오류 발생 시 에러 메시지가 표시되고 앱이 크래시하지 않는다.
- [ ] API 키가 클라이언트 코드/네트워크 탭에 노출되지 않는다.

## 10. 구현 참고 사항
- OpenRouter 모델 slug는 시점에 따라 변경/종료될 수 있으므로, 실제 구현 시 https://openrouter.ai/models 에서 `google/gemma-4-26b-a4b-it:free` 의 현재 유효성(및 vision 입력 지원 여부)을 재확인할 것을 권장한다.
- 무료 티어 모델은 rate limit이 엄격할 수 있으므로 재시도/큐잉 전략을 고려한다.
