# PRD - Step 2: 식재료 기반 레시피 생성

## 1. 개요
1단계에서 확정된 식재료 목록을 입력으로 받아, OpenRouter API의 `nvidia/nemotron-3-ultra-550b-a55b:free` 모델을 이용해 사용자가 만들 수 있는 레시피(요리법)를 생성하는 기능. 1단계가 "무엇이 있는가"를 파악했다면, 2단계는 "이걸로 무엇을 만들 수 있는가"에 답한다.

## 2. 목표
- 확보한 재료만으로(또는 최소한의 추가 구매로) 만들 수 있는 레시피를 자동 추천한다.
- 레시피는 실제로 따라 할 수 있도록 재료 목록, 조리 순서, 예상 소요 시간을 포함한다.

## 3. 사용자 스토리
- 사용자로서, 인식된 재료를 기반으로 오늘 저녁에 뭘 해먹을지 추천받고 싶다.
- 사용자로서, 여러 레시피 후보 중 마음에 드는 걸 골라 상세 조리법을 보고 싶다.
- 사용자로서, 냉장고에 없는 재료가 있다면 무엇을 추가로 사야 하는지 알고 싶다.

## 4. 범위 (In Scope)
- 1단계 결과(재료 리스트)를 입력받아 레시피 생성 요청
- 사용자 선호도 입력(선택 사항): 조리 난이도, 인원 수, 알레르기/제외 재료, 요리 종류(한식/양식/중식 등)
- 레시피 후보 여러 개(예: 3개) 생성 및 카드 형태로 목록 표시
- 레시피 상세보기: 재료 목록(보유/부족 구분), 조리 순서, 예상 조리 시간, 난이도
- 레시피 재생성(다른 추천 받기) 기능

## 5. 범위 제외 (Out of Scope)
- 사용자 프로필/로그인, 레시피 저장 (3단계에서 다룸)
- 영양 정보 계산, 칼로리 분석
- 레시피 이미지 생성

## 6. 기능 요구사항

### 6.1 레시피 생성 요청
- 입력 데이터: 1단계에서 확정된 재료 리스트(JSON), 선택적 사용자 선호도
- 백엔드에서 OpenRouter Chat Completions API 호출
  - 모델: `nvidia/nemotron-3-ultra-550b-a55b:free`
  - 프롬프트 예시: "다음 재료 목록을 최대한 활용하는 요리 레시피 3가지를 추천하라. 각 레시피는 이름, 필요한 재료(보유 재료와 매칭), 부족한 재료, 조리 순서(단계별), 예상 조리 시간, 난이도(하/중/상)를 포함한 JSON으로만 응답하라."
  - 기대 응답 스키마 예시:
    ```json
    {
      "recipes": [
        {
          "title": "계란파채볶음밥",
          "difficulty": "하",
          "cookTimeMinutes": 15,
          "servings": 2,
          "usedIngredients": ["계란", "대파", "밥"],
          "missingIngredients": ["간장"],
          "steps": [
            "대파를 잘게 썬다.",
            "팬에 기름을 두르고 계란을 스크램블한다.",
            "..."
          ]
        }
      ]
    }
    ```
- 사용자 선호도(제외 재료, 인원 수 등)가 있으면 프롬프트에 반영
- 모델 응답 JSON 파싱 실패 시 재시도 또는 에러 처리 (1단계와 동일한 패턴)

### 6.2 레시피 목록 및 상세 화면
- 레시피 카드: 제목, 난이도, 조리 시간, 대표 재료 미리보기
- 카드 클릭 시 상세 화면: 전체 재료(보유/부족 구분 표시), 단계별 조리 순서, 팁(선택)
- 부족한 재료는 별도로 강조 표시 ("장보기 목록"으로 활용 가능하도록)

### 6.3 재생성 및 필터
- "다른 레시피 추천받기" 버튼 → 동일 재료로 재요청 (다양성 확보를 위해 temperature 조정 또는 이전 추천 제외 프롬프트 추가)
- 선호도 필터 변경 후 재요청 가능

### 6.4 에러 처리
- 재료 리스트가 비어있을 경우 레시피 생성 요청 비활성화
- OpenRouter API 오류/레이트리밋 시 사용자 친화적 메시지 및 재시도 옵션 제공

## 7. 비기능 요구사항
- 레시피 생성 응답 시간 목표: 평균 15초 이내 (무료 모델 특성상 변동 가능성 있음을 UI에 안내 문구로 명시)
- API 키는 서버 측에서만 사용, 클라이언트 미노출
- 1단계와 동일하게 백엔드를 경유하여 OpenRouter 호출

## 8. 화면 구성 (초안)
1. 레시피 추천 화면: 확정된 재료 리스트 요약 + (선택) 선호도 입력 폼 + "레시피 추천받기" 버튼
2. 레시피 목록 화면: 추천된 레시피 카드 3개 + "다시 추천받기" 버튼
3. 레시피 상세 화면: 재료(보유/부족), 조리 순서, "저장하기" 버튼(3단계 진입, 현재는 비활성 또는 placeholder)

## 9. 완료 기준 (Acceptance Criteria)
- [ ] 1단계에서 넘어온 재료 리스트로 레시피 생성 요청이 가능하다.
- [ ] 최소 3개의 레시피 후보가 카드 형태로 표시된다.
- [ ] 레시피 상세 화면에서 보유 재료와 부족 재료가 구분되어 표시된다.
- [ ] 조리 순서가 단계별로 명확하게 표시된다.
- [ ] "다시 추천받기"로 새로운 레시피 세트를 받을 수 있다.
- [ ] API 오류 시 앱이 크래시하지 않고 에러 메시지가 표시된다.

## 10. 구현 참고 사항
- `nvidia/nemotron-3-ultra-550b-a55b:free` 역시 실제 구현 전 https://openrouter.ai/models 에서 모델 slug 유효성과 컨텍스트 길이(재료 리스트+프롬프트가 충분히 들어가는지)를 확인할 것을 권장한다.
- 두 모델 모두 무료 티어이므로 동시 사용 시 계정 단위 레이트리밋에 걸릴 수 있어, 1단계-2단계 연속 호출 시 사용자에게 대기 상태를 명확히 안내한다.
